// This is an example TypeScript code to demonstrate basic functionality.
// It's not meant for production use or complex logic yet.

function add(a: number, b: number): number {
    return a + b;
}

let result = add(5, 3);
console.log(result); // Output: 8
